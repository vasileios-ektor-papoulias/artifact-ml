name: CI_PUSH[MAIN]

on:
  push:
    branches:
      - 'main'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-message:
    if: ${{ !contains(github.event.head_commit.message, '[skip ci]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Lint merge commit message
        run: |
          chmod +x .github/scripts/linting/*.sh
          .github/scripts/linting/lint_merge_commit_message.sh

  lint-description:
    if: ${{ !contains(github.event.head_commit.message, '[skip ci]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Lint merge commit description
        run: |
          chmod +x .github/scripts/linting/*.sh
          .github/scripts/linting/lint_merge_commit_description.sh

  ci-component:
    needs: [lint-message, lint-description]
    if: ${{ !contains(github.event.head_commit.message, '[skip ci]') }}
    permissions:
      contents: read
      pull-requests: write  # Required for Codecov/SonarCloud PR comments
      id-token: write  # Required for Codecov OIDC
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - component: artifact-core
            module: artifact_core
            flag: core
          - component: artifact-experiment
            module: artifact_experiment
            flag: experiment
          - component: artifact-torch
            module: artifact_torch
            flag: torch
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Check if component changed
        id: changes
        run: |
          chmod +x .github/scripts/ci/check_component_changed.sh
          RESULT=$(.github/scripts/ci/check_component_changed.sh "${{ matrix.component }}")
          echo "has_changes=$RESULT" >> $GITHUB_OUTPUT
          if [ "$RESULT" = "true" ]; then
            echo "✅ Component ${{ matrix.component }} has changes"
          else
            echo "⏭️ Component ${{ matrix.component }} has NO changes - skipping CI"
          fi

      - name: Set up Python
        if: steps.changes.outputs.has_changes == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Poetry
        if: steps.changes.outputs.has_changes == 'true'
        uses: snok/install-poetry@v1
        with:
          version: latest
          virtualenvs-create: true
          virtualenvs-in-project: false

      - name: Cache Poetry dependencies
        if: steps.changes.outputs.has_changes == 'true'
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/pypoetry
            ~/.local/share/pypoetry
          key: ${{ runner.os }}-poetry-${{ matrix.component }}-${{ hashFiles(format('{0}/poetry.lock', matrix.component)) }}
          restore-keys: |
            ${{ runner.os }}-poetry-${{ matrix.component }}-

      - name: Install Dependencies
        if: steps.changes.outputs.has_changes == 'true'
        run: |
          cd ${{ matrix.component }}
          poetry install --with dev

      - name: Run Linting with Ruff
        if: steps.changes.outputs.has_changes == 'true'
        run: |
          cd ${{ matrix.component }}
          poetry run ruff check .

      - name: Run Unit Tests with Pytest
        if: steps.changes.outputs.has_changes == 'true'
        run: |
          cd ${{ matrix.component }}
          poetry run pytest \
            --cov="${{ matrix.module }}" \
            --cov-report=term-missing \
            --cov-report=xml:coverage.xml
      
      - name: Upload coverage to Codecov
        if: steps.changes.outputs.has_changes == 'true'
        uses: codecov/codecov-action@v4
        with:
          files: ${{ matrix.component }}/coverage.xml
          flags: ${{ matrix.flag }}
          use_oidc: true
          fail_ci_if_error: true
      
      - name: SonarCloud Scan
        if: steps.changes.outputs.has_changes == 'true'
        uses: SonarSource/sonarqube-scan-action@v6
        with:
          projectBaseDir: ${{ matrix.component }}
        env:
          SONAR_HOST_URL: https://sonarcloud.io
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      - name: Build Package
        if: steps.changes.outputs.has_changes == 'true'
        run: |
          cd ${{ matrix.component }}
          poetry build

  bump-version:
    needs: ci-component
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: ${{ !contains(github.event.head_commit.message, '[skip ci]') }}
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT_TOKEN }}
          fetch-depth: 2

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          version: latest
          virtualenvs-create: true
          virtualenvs-in-project: false

      - name: Run Version Bump Job
        run: |
          # Ensure all scripts are executable
          chmod +x .github/scripts/version_bump/*.sh
          
          # Run the version bump job script
          .github/scripts/version_bump/job.sh
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
