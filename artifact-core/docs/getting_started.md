# Getting Started  

<p align="center">
  <img src="../assets/artifact_ml_logo.svg" width="200" alt="Artifact-ML Logo">
</p>  

## Installation

Install the latest [`artifact-core`](https://pypi.org/project/artifact-core/) release from PyPI by running:

```bash
pip install artifact-core
```

## Installing from Source (Development)

To work on `artifact-core` itself, clone the [Artifact-ML monorepo](https://github.com/vasileios-ektor-papoulias/artifact-ml/tree/main) by running:

```bash
git clone https://github.com/vasileios-ektor-papoulias/artifact-ml.git
```

Then install the package (with dev dependencies) using [Poetry](https://python-poetry.org/):

```bash
cd artifact-ml/artifact-core

poetry install --with dev
```
